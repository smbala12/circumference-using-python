# ⭕ Circumference Calculator (Python)

A simple Python program that calculates the **circumference of a circle** using a user-provided radius.

---

## 📌 How It Works

The program:

1. Imports the built-in `math` module.
2. Prompts the user to enter the radius (`R`) of a circle.
3. Uses the value of π (pi) from the `math` module.
4. Applies the circumference formula:

   Circumference = 2 × π × R

5. Rounds the result to 2 decimal places.
6. Displays the final circumference with units.

---

## ▶️ How to Run

1. Make sure Python is installed on your computer.
2. Save the file as `circumference.py`
3. Open a terminal or command prompt.
4. Run:

```bash
python circumference.py
